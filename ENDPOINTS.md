# Backend Endpoints needed

"->" = return

1. connectToWombat(ip) -> jwtToken wahrscheinlich?
   2. Wird irgendiwe abgespeichert als history
2. getStatusOfWombat(ip) -> batteryPercentage
   3. wenn battery 0 dann is er automatisch offline
4. createNewProject(name) -> void
5. deleteProject(name) -> void
6. openProject(name) -> flowchart irgendwie
   7. noch ausmachen
8. saveProject(name ,changed Fields oder so)
9. runProject(name) -> (stream wo er gerade ist)
